// 11. Comma Operator

// Executes multiple expressions and returns the last one.

let val = (1 + 2, 3 + 4);
console.log(val); // 7

// The comma operator allows:
        //  multiple expressions in one statement
// But it returns:
        // ONLY the last expression value   