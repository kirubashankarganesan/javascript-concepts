// join() converts array into string by adding separator between values.

let arr = [1,2,3,4,5]

let joinedVal = arr.join(" ")
// array.join(separator)
// 1 2 3 4 5
//---------------------------------

// tostring- Converts array into comma separated string.
console.log( joinedVal );

let stringVal = arr.toString("12")

console.log( stringVal );
// 1,2,3,4,5